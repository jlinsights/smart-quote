interface:
  display_name: "Smart Quote"
  short_description: "Repo-specific patterns and workflows for smart-quote"
  default_prompt: "Use the smart-quote repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true