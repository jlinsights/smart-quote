# bridgelogis.com — robots.txt
# Last updated: 2026-05-10
# Powered by: Goodman GLS

# Default rule — applies to all crawlers (incl. AI bots like GPTBot,
# PerplexityBot, ClaudeBot, Google-Extended, Bingbot) unless they have
# their own block below. We allow public content so the product can be
# cited in AI-generated answers.
User-agent: *
Allow: /

# Block authenticated / non-public areas
Disallow: /admin
Disallow: /admin/
Disallow: /dashboard
Disallow: /dashboard/
Disallow: /schedule
Disallow: /schedule/
Disallow: /api/
Disallow: /login
Disallow: /signup

# Block search-result-style URLs (avoid duplicate-content issues)
Disallow: /*?q=*
Disallow: /*?search=*

# Crawl delay for aggressive 3rd-party crawlers
User-agent: AhrefsBot
Crawl-delay: 5

User-agent: SemrushBot
Crawl-delay: 5

User-agent: MJ12bot
Crawl-delay: 10

# Note: Googlebot/Yeti/Daum follow the User-agent: * rules above.
# Bot-specific groups were removed to prevent accidental override of the
# global Disallow rules (a bot-specific group with only `Allow: /` would
# otherwise let those crawlers reach /admin, /api, etc.).

# Sitemaps
Sitemap: https://bridgelogis.com/sitemap.xml

# AI-agent context files
# llms.txt — product context for LLMs (https://llmstxt.org)
# pricing.md — machine-readable pricing tiers for AI agents
# Both are linked here so AI crawlers can find them without guessing paths.
# https://bridgelogis.com/llms.txt
# https://bridgelogis.com/pricing.md
